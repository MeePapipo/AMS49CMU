-- ล้างข้อมูลทั้งหมดในฐาน ให้เหลือตารางเปล่า ๆ (โครงตารางยังอยู่ครบ)
--
-- ใช้กับฐานจำลองในเครื่อง:  npm run db:clear
-- อยากได้ข้อมูลตัวอย่างกลับมา:  npm run db:seed
--
-- ห้ามรันกับฐานจริง — ไม่มี undo และ D1 แผนฟรีไม่มี point-in-time restore
-- (ถ้าจำเป็นต้องล้างของจริงจริง ๆ ให้ export ออกมาเก็บก่อน ดู DEPLOY.md หัวข้อสำรองข้อมูล)
--
-- หมายเหตุ: ไฟล์สลิปอยู่ใน R2 คนละที่กับตารางนี้ ลบแถวแล้วไฟล์ยังค้างอยู่
-- ตอน dev ล้าง R2 ด้วยการลบโฟลเดอร์ .wrangler/state/v3/r2 แล้วรัน dev ใหม่

DELETE FROM orders;
DELETE FROM audit;
DELETE FROM rate;
